git clone https://github.com/emilyyyliuu/qtm350-quiz01.git
cd qtm350-quiz01
: > commands.txt
git checkout -b feature-update
mkdir -p reports
touch reports/summary.md
echo "This document provides a summary of the project updates." >> reports/summary.md
git add reports
git commit -m "Add reports directory"
touch reports/file{1..3}.txt
mv data/raw-data.csv data/input-data.csv
mkdir -p scripts/backup && cp scripts/*.py scripts/backup/
rm docs/documentation.md
git add -A
git commit -m "Update project files"
printf "temp/\ndataset0?.csv\n" >> .gitignore
cat .gitignore
git add .gitignore
git commit -m "Add .gitignore file"
git checkout main
git merge feature-update
git add commands.txt
git commit -m "Add commands log"
git push -u origin main
git remote add upstream https://github.com/danilofreire/qtm350-quiz01.git
git push origin main
git rebase -i HEAD~2
git push origin main --force
nano .git/hooks/pre-commit

chmod +x .git/hooks/pre-commit
git commit -m "Test commit"
echo "Hook test" >> README.md
git add README.md
git commit -m "Update README for hook test"
